Shows where a scan is in its lifecycle. Do not animate `processing` unless real processing is happening.

```jsx
<StatusPill status="processing" />
<StatusPill status="ready" label="Ready · 12 Mar" />
```
