The site header. Use `transparent` when it overlays a full-bleed terrain hero.

```jsx
<NavBar items={['About','Services','Projects','Contact']} active="Services" transparent assetBase="../../" />
```
